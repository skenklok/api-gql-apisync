type Query {
  getWeather(city: String!): Weather
  album(artist: ID!): Album
}

type Weather {
  city: String
  temperature: Float
  condition: String
  description: String
}

type Album {
  id: ID!
  name: String!
  artist: String!
  artworkUrl: String!
  releaseDate: String!
}

